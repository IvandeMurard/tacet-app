---
id: story-4.4
tac: TAC-33
title: "Manifest, icons, and installability"
epic: "Epic 4: Progressive Web App & Offline"
status: ready
blockedBy: null
depends: [story-4.1]
priority: must
storyPoints: 2
---

# Story 4.4: Manifest, icons, and installability

## User Story

As a product owner,
I want the PWA manifest and icons to be correct so that "Add to Home Screen" works on mobile and desktop,
So that users can install Tacet without an app store (FR28).

## Acceptance Criteria

**Given** the app has a manifest (e.g. public/manifest.json or generated)
**When** the manifest is validated (e.g. Lighthouse PWA or browser devtools)
**Then** name, short_name, start_url, display (standalone or minimal-ui), and lang (fr) are set
**And** icons include at least 192px and 512px (and maskable if required)
**When** the user visits the app on a supporting device/browser
**Then** the install option appears (browser UI or custom prompt)
**And** after install, launch shows the app shell and map as expected

## Technical Context

- manifest.json in public/ or generated by Serwist/Next. name, short_name, start_url (e.g. /), display: standalone or minimal-ui, lang: fr. Icons: 192, 512, maskable if required.
- Layout or document: link rel="manifest". Icons in public/icons/. Lighthouse PWA audit: manifest valid, installable.

## Affected Files

- `tacet/public/manifest.json` (or Serwist-generated)
- `tacet/public/icons/` (192, 512, maskable)
- `tacet/src/app/layout.tsx` (manifest link, theme_color if needed)

## Dependencies

- Story 4.1 (service worker). Manifest and icons can be done in parallel with 4.1.

## Testing Notes

- Lighthouse PWA: manifest valid; installable; icons present. Manual: Add to Home Screen on mobile/desktop; launch → shell and map.
